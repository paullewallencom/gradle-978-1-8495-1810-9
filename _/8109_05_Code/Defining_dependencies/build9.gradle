apply plugin: 'java'

def springGroup = 'org.springframework'
def springRelease = '3.1.1.RELEASE'

dependencies {
    compile module("$springGroup:spring-context:$springRelease") {
        dependency("$springGroup:spring-aop:$springRelease") {
            transitive = false
        }
    }
}
